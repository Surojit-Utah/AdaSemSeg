# Datasets (*.npy) and model weights (*.pth.tar, *.pth) are not committed here
# -- both are downloaded from Zenodo, see scripts/download_assets.py and the
# README's "Download datasets and weights" / "Model weights" sections.
